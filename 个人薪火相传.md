沉淀积累
1. 转换思路，深入了解高频技术知识
2. 吃透项目的核心思想以及高可用、高性能、高保障的手段和措施
3. 数据结构 + 算法
个人介绍
msg你好，我叫李浩，从2024年通过校招加入阿里巴巴菜鸟驿站工作至今，一共有2年的Java后端开发经验：
在菜鸟驿站，我先后负责过两个项目：
1. 第一个是菜鸟末端无人车项目，从0到1构建了无人车履约任务模型、以及抽象了无人车核心资产模型，帮助网点、站点进行无人车包裹运输实操、车辆调度、资产管理等功能
2. 第二个是网点驿站实操项目，从0到1构建了包裹入库错分链路模型，覆盖五家主流快递公司，同时通过双单元LDB + 前置client + 异步提前处理，将查询包裹错分rt降低到10ms内；另外优化了特殊件模型，使特殊件支持灰度；设计主动隔离 + 被动隔离 + 消息补偿的方式提高了推送物流轨迹主链路高可用
以上是我的自我介绍，谢谢

无人车
资产树、资产链
根据root + bizType 锁定同一业务上的所有资产（比如装货区）



推送物流轨迹、特殊件qps
物流轨迹 qps 11134

特殊件 qps 8000



数据迁移方案
数据迁移的两种主流在线方案：流量双写 和 基于 binlog 的增量追平，都能实现停机时间极短甚至零停机的迁移。它们的核心区别在于：谁负责同步数据——业务代码还是基础设施。

一、流量双写（Double Write）
1. 原理
应用程序同时向新、老两个数据源写入数据，保证迁移期间两边数据实时一致；历史数据通过离线工具补齐，最后将读流量切换到新库。
2. 迁移步骤
代码改造：在 DAO 层增加双写逻辑，所有增删改操作同时写老库和新库。
历史数据迁移：用离线工具（如 DataX、Sqoop）将存量数据全量同步到新库。
数据校验：持续对比新老库数据，修复不一致。
切读：将读流量灰度切换到新库（先部分，再全量）。
下线：观察一段时间后，关闭双写，下线老库。
3. 案例
从自建 MySQL 迁移到云数据库 RDS
业务团队在订单服务中增加双写逻辑：insertOrder 同时写老 MySQL 和 RDS。
夜间用 DataX 把三个月前的历史订单同步到 RDS。
校验通过后，DNS 将读流量逐步切到 RDS。
稳定运行一周后，移除双写代码，下线老 MySQL。
4. 优缺点
✅ 优点：不依赖数据库日志，可跨数据库类型（如 MySQL → PostgreSQL）；可控性强，可回滚。
❌ 缺点：侵入业务代码，双写期间性能下降，需处理双写失败（如某一边写成功另一边失败）导致的数据不一致，需引入补偿机制。

二、基于 binlog 增量追平（CDC 方案）
1. 原理
利用数据库的 binlog（MySQL）/ redo log（Oracle） 捕获增量变更，通过 CDC（Change Data Capture） 工具（如 Canal、Debezium、DTS）实时同步到新库，先全量再增量，最终追平延迟后切换。
2. 迁移步骤
全量迁移：使用 mysqldump 或 XtraBackup 物理备份老库，恢复至新库，记录备份时的 binlog 位点。
开启增量同步：部署 Canal Server，解析从该位点开始的 binlog，将增量数据实时写入新库（如 Kafka → 消费端写入新库）。
追平延迟：监控同步延迟，当延迟稳定在秒级或毫秒级时，准备切换。
校验：数据对比工具（如 pt-table-checksum）验证一致性。
切换：短暂停写老库（或设置只读），等待剩余增量全部同步，然后将应用流量切到新库。
下线：关闭同步任务，下线老库。
3. 案例
MySQL 迁移到 TiDB（或同构迁移到 RDS）
使用 TiDB 的 DM（Data Migration）工具，先全量备份恢复，再解密 MySQL binlog 实时同步。
延迟追平到 0 后，夜间将老库设为只读，最后一批增量同步完成，应用配置切换。
整个停机时间仅 2 分钟。
使用阿里云 DTS 迁移 MySQL 到 PolarDB
DTS 自动完成全量 + 增量，无需人工干预，支持双向同步，切换瞬间完成。
4. 优缺点
✅ 优点：对业务代码零侵入，同步高效，延迟低；可支持异构数据库（需兼容）。
❌ 缺点：依赖 binlog 格式（通常需要 ROW 格式），大事务、DDL 同步需特殊处理；工具链复杂，需维护 Canal/Kafka 等组件。

三、对比总结
全屏复制
维度	流量双写	binlog 增量追平
业务侵入性	高（需修改代码）	无（业务无感知）
数据一致性	最终一致，需处理双写失败	强一致，可追平到秒级
跨数据库类型	容易	受限于 binlog 格式和目标库兼容性
性能影响	双写增加 RT，可能降低吞吐	仅对源库有轻微 binlog 读取压力
切换停机时间	可做到零停机（灰度切读）	通常需短暂停写（秒～分钟）
适用场景	跨异构数据库、无法使用 binlog 的场景	同构/兼容数据库，追求低侵入

一句话总结：
想对业务透明、快速迁移，优先选 binlog 增量追平。
需要跨数据库类型、无法使用 binlog，或已有成熟双写框架，选 流量双写。

主动隔离 + 被动隔离 保证业务稳定性
● 设置了异常报警，第一时间发现某个cp不稳定，消息先被动隔离，如果大批量则主动隔离
● 设置主动隔离配置，如果cp下游不稳定，则直接将对应cp的物流轨迹推送放入到新的topic中处理，不影响主链路
● 被动隔离，设置cp对应cp，如果推送物流轨迹异常，则发送消息到异常topic处理
  ○ 推送物流成功，删除数据库中异常记录
  ○ 推送物流轨迹link成功，但是业务失败，如果cp开启隔离，则发送消息到异常topic处理；如果未开启，则异常轨迹落库，则进行消息重试
  ○ 推送物流轨迹link失败，如果cp开启隔离，则发送消息到异常topic处理，如果未开启，则异常轨迹落哭，进行消息重试
● 定时补偿任务 30分钟扫描一次：com.cainiao.terminal.gateway.schedulex.RetryPushLgToCpDtsJob


压测保障
压测要求：
● QPS > 1500
● 成功率 > 99.95%
● RT < 200 ms
● 持续压测时间 > 1小时
● 低峰时间压测
包裹出入库节点消息回传cp，cp接口错误率小于0.05%，cp接口平均RT小于200ms，压测持续一个小时以上

CP压测QPS：
获取往年618、双11 接口的QPS峰值参照对比
压测QPS = 日常QPS * 安全系数（1.5 - 2.0）

报警处理：
1. 联系cp和业务，及时关注
2. 查看系统水位，如果持续5分钟水位高于60%，则进行扩缩容，没有自动扩缩，则手动
3. 如果依然没有解决，5分钟cpu水位持续高于60%，10分钟接口平均RT > 600ms，5分钟接口成功率 < 90%
  a. 联系cp，进行限流，按照要求-10%、-20%、-30%、-40%、-50%进行限流
  b. by CP进行sentinel限流，保证按照下游可承受qps进行消费调用
  c. 接口、水位、cpu正常后，逐步恢复到大促峰值限流值
  d. 主动调用定时任务，获取抖动期间推送轨迹失败的case，重新推送

异常Case：
Case1，持续5分钟调用量>50000且10分钟内平均rt>600ms
Case2，持续2分钟调用量>10000且成功率<90%

压测放量流程：




高峰双十一单量暴涨，有做什么优化或者什么保障手段吗
事前：
1. 大促前压测，模拟高峰QPS，单机压测、数据库压测
2. 提前扩容，应用扩容、数据库：读写分离、缓存扩容
3. sql治理：索引优化、历史数据归档
事中：
1. 限流降级
Sentinel 限流：对非核心接口设置 QPS 上限，保护核心链路
服务降级：非核心功能（如满意度调查、考核评估）可降级关闭
Diamond or switch开关：通过配置中心快速开关功能
2. 缓存策略
热点数据预热：大促前将热点数据提前加载到 Tair 缓存
多级缓存：本地缓存 + 分布式缓存，减少 DB 压力
缓存兜底：DB 不可用时，用缓存数据兜底返回
热key发现：Sentinel发现热key, 短暂本地缓存返回
3. 异步化
消息削峰：用 MetaQ 将同步调用改为异步消息，削平流量尖峰
延迟处理：非实时业务（如仲裁通知、数据统计）延迟到低峰期处理，比如插入一条操作记录到db, 后面定时去扫描补偿
4. 预案
设置降级页面或者公告，开关控制

监控告警：
实时大盘：QPS、RT、错误率、CPU、内存、DB 连接数
秒级告警：核心指标异常秒级触达值班人员
自动扩缩容：基于 CPU/QPS 指标自动弹性伸缩
预案体系：
预案级别	触发条件	操作
P1	CPU > 80% 或 RT 翻倍	限流非核心接口
P2	数据库连接池打满	降级非核心查询，走缓存
P3	系统不可用	切流到备用集群 / 静态兜底页

包裹轨迹推送
峰值QPS 11000
单cp较多流量的QPS 为 1500
压测按照 2250 QPS 压测
8台压机器

压测计划
脉冲压测：
0~10m:   QPS 从 0 飙到 1500（零点脉冲）
10~30m:  维持 1500
30~60m:  上升到 2250
60~90m: 维持2250
90-120m之后回落到 1500
再回落到0

热点压测：
模拟 10% 的请求集中到 1 个站点（同一分片）
验证：
  - 热点分片的 DB 是否扛得住
  - 其他分片是否受影响
  - 热点隔离策略是否生效
故障演练
故障场景	验证目标
杀掉 1 台 MySQL	流量是否自动切到从节点
Redis 主节点宕机	哨兵切换是否正常，期间请求是否报错
MQ Broker 宕机	消息是否丢失，补偿机制是否生效
应用实例缩容 50%	剩余实例是否能承接全部流量

限流500qps
看监控大盘
影子标原理：RPC上下文存放影子标，MyBatis 拦截器实现走带_test的表


Redo Log一直写的原因
1. 事务提交前：用 Redo Log 保护 Undo Log，确保能回滚。
  ○ 场景： 脏页被提前刷盘，未提交事务就写入了磁盘。
  ○ Redo Log 的作用： 保证 Undo Log 页的完整性。这样 MySQL 崩溃重启后，能读到正确的 Undo Log，把磁盘上的未提交修改回滚掉。
2. 事务提交后：用 Redo Log 本身，确保能重做。
  ○ 场景： 事务已提交，但脏页还没来得及刷盘。
  ○ Redo Log 的作用： 直接重放已提交事务的修改。这样 MySQL 崩溃重启后，能把提交成功但丢失的数据，重新恢复到磁盘上。
一句话总结：
Redo Log 一直写，是为了在崩溃发生时，无论数据页处于“已提交但未刷盘”还是“未提交但已刷盘”的状态，都能让数据库恢复到正确的状态。

Mysql 三大日志
● Redo Log
  ○ 物理日志、重做日志，记录了磁盘上物理页修改的数据
  ○ 主要用作崩溃恢复
  ○ 事务开启时就会一直写，首先写入redo log buffer
  ○ 刷盘机制
    ■ 0：提交事务不刷盘，由后台线程将redo log buffer刷盘到磁盘 
      ● mysql 挂了 、服务器宕机会丢1s数据
    ■ 1：提交事务就刷盘，将redo log buffer 刷盘到磁盘
      ● 不会丢失数据
    ■ 2:  事务提交，将redo log buffer 刷到page cache，由操作系统线程每隔1s刷盘一次
      ● mysql挂了没事，服务器宕机会丢1s数据
● Bin Log
  ○ 逻辑日志，记录的是sql，用作主从数据同步
  ○ 记录格式
    ■ statement：原sql
    ■ row：如果记录原sql在不同时间插入数据不一致，则会替换那些动态函数，将原值记录下来
    ■ mixed：判断记录原sql会不会造成数据不一致，不会则记录原sql，否则记录row
  ○ 刷盘时机
    ■ 0：当事务提交，将binlog cache写入page cache，由操作系统后台线程1s刷盘一次（fsync)
      ● 如果mysql挂了不会丢数据，服务器宕机丢1s数据
    ■ 1：当事务提交，即将binlog cache刷盘到磁盘
    ■ N：当事务提交n个后，则进行刷盘
● Undo Log
  ○ 回滚日志（逻辑日志），主要用来事务回滚和MVCC
  ○ 如果是Delete，则记录Insert，如果是Update，则记录新旧值
  ○ 构成Undo Log 版本链，控制事务之间隔离性
  ○ nsert Undo Log：格式简单，只记主键，提交即删，不参与版本链。
  ○ Update Undo Log：格式复杂，记录旧值、事务ID、回滚指针，是版本链的物理节点，必须等 Purge 线程确认无引用后才能删除。
┌──────────────────────────────────────────────────┐
│  Undo Log Header（日志头）                        │
│  - 记录类型（Insert / Update）                    │
│  - 下一条 Undo 记录的偏移量（同一个 Undo 页内）    │
├──────────────────────────────────────────────────┤
│  Undo Log Record Body（日志体）                   │
│  - Table ID                                      │
│  - 主键信息                                       │
│  - DB_TRX_ID（事务ID）                           │
│  - DB_ROLL_PTR（回滚指针，指向更早的版本）        │
│  - 被修改字段的旧值                               │
└──────────────────────────────────────────────────┘

两阶段提交（2PC)
● 将提交Redo Log分成prepare、commit阶段
● 通过Redo log恢复数据顺序
  ○ 判断Redo log 阶段是否是commit，是则进行数据恢复
  ○ 如果redo log当前阶段是prepare，则通过事务id查询binlog 是否有已提交的事务，如果有则进行数据恢复，如果没有，则不恢复数据
总结：这个顺序保证了：只要 Binlog 写了，Redo Log 就一定能提交；只要 Binlog 没写，Redo Log 就一定会回滚。 这就是“Redo 先刷，Binlog 再刷，Redo 最后确认”的根本原因。


索引下推
正常查询流程：
Mysql分析器根据最适合的索引进行查询 -> 存储引擎将查询到的数据返回给server -> server根据其他字段进行过滤
如果命中联合索引，存储引擎会根据索引条件（包括 ICP 下推的条件）在存储引擎层先做一轮过滤，然后回表拿到完整数据，返回给 Server 层。Server 层再对存储引擎无法处理的剩余条件（如不在索引中的字段）进行最终过滤。

如果有联合索引，且命中，可以直接通过索引下推，在存储引擎层，直接把数据过滤，而减少回表次数
完整执行顺序（以联合索引 (a, b, c) 为例）
假设 SQL 是：
sql


SELECT * FROM table WHERE a = 1 AND b > 10 AND c = 5 AND d = 'hello';
其中 (a, b, c) 是联合索引，d 是普通字段，没有索引。
步骤 1：Server 层分析 SQL，决定使用哪个索引
MySQL 的优化器会根据统计信息和查询条件，选择最优的索引。
这里命中了联合索引 (a, b, c)。
步骤 2：存储引擎根据联合索引进行查找
联合索引的匹配规则是最左前缀原则：
a = 1：等值匹配，命中索引第一列。
b > 10：范围匹配，命中索引第二列。
c = 5：由于 b 是范围查询，联合索引在 b 这里就停止匹配了。所以 c 无法用于索引的精确查找，但 c 的值仍然存在于索引记录中。
这里分两种情况：

情况 A：没有开启 ICP（索引条件下推）
存储引擎只使用 a = 1 AND b > 10 来扫描索引，找到所有符合这个条件的主键。
然后拿着主键去回表，读取完整的行数据（包括 c 和 d 字段）。
把所有完整行数据返回给 Server 层。
Server 层再根据 c = 5 AND d = 'hello' 进行过滤，只保留符合所有条件的行。
问题： 如果 b > 10 扫描了大量数据，但 c = 5 过滤掉了大部分，那么存储引擎会做很多无用的回表操作，浪费 IO。

情况 B：开启了 ICP（索引条件下推，MySQL 5.6+ 默认开启）
存储引擎依然只使用 a = 1 AND b > 10 来扫描索引。
但是，在扫描索引时，存储引擎会直接检查索引记录中的 c 字段，判断 c = 5 是否成立。
如果 c != 5，直接跳过这条记录，根本不回表。
只有 c = 5 成立时，才去回表读取完整行数据，然后返回给 Server 层。
Server 层再根据 d = 'hello' 进行最后的过滤（因为 d 不在联合索引中，存储引擎无法判断）。
好处： 大大减少了回表次数，提升了查询性能。

Mysql隔离级别
● 读未提交
  ○ 出现脏读、不可重复读、幻读问题
● 读已提交（RC）
  ○ 出现不可重复读、幻读问题
● 可重复读（RR）
  ○ 出现幻读问题
  ○ 通过MVCC解决不可重复读问题
  ○ 通过Next-key Lock（Record Lock+Gap Lock）解决幻读问题
● 串行化
解决幻读问题的本质：
● 在某个事务执行的时候，不允许其他事务对当前事务操作的数据范围做增删
● 解决幻读方法
  ○ 隔离级别设置为串行化
  ○ RR级别下，给事务操作的表加表锁
  ○ RR界别下，给事务操作的数据范围添加Next-key Lock（Record Lock+Gap Lock）

Mysql锁
意向锁
● 解决表锁和行锁冲突问题，加表锁的时候先看一下有没有意向锁
● 添加行锁的时候，先添加意向锁，意向排他锁或意向共享锁
意向锁之间是互相兼容的。
	IS 锁	IX 锁
IS 锁	兼容	兼容
IX 锁	兼容	兼容
意向锁和共享锁和排它锁互斥（这里指的是表级别的共享锁和排他锁，意向锁不会与行级的共享锁和排他锁互斥）。
	IS 锁	IX 锁
S 锁	兼容	互斥
X 锁	互斥	互斥

● 表锁
  ○ MDL（Metadate Lock）
    ■ 元数据锁
  ○ 自增锁（添加新数据，保证主键盘id自增）
  ○ 意向锁
    ■ 意向共享锁
    ■ 意向排它锁
锁类型	全称	作用	兼容性
MDL_SHARED	共享锁	CRUD 操作时自动加	读读兼容
MDL_EXCLUSIVE	排他锁	ALTER TABLE 时加	和任何锁都互斥
● 行锁
  ○ Record Lock
  ○ Gap Lock
  ○ Next-Key Lock 解决幻读问题

MVCC
● ReadView、每行数据的隐藏字段（最后修改当前记录的事务id）、undo log 版本链
字段	含义
m_ids	当前活跃事务的 ID 集合（正在执行但未提交的事务）
min_trx_id	活跃事务中最小的事务 ID
max_trx_id	系统下一个要分配的事务 ID（即全局最大事务 ID + 1）
creator_trx_id	创建这个 ReadView 的事务 ID

可见性判断
● 如果 trx_id == creator_trx_id→ 可见（这是我自己的修改）
● 如果 trx_id < min_trx_id→ 可见（修改这行的事务在我之前就提交了）
● 如果 trx_id >= max_trx_id→ 不可见（修改这行的事务在我之后才开启）
● 如果 min_trx_id <= trx_id < max_trx_id→ 检查 trx_id 是否在 m_ids 中
  ○ ├── 在 m_ids 中 → 不可见（事务未提交）
  ○ └── 不在 m_ids 中 → 可见（事务已提交）


Mysql 隔离级别为什么设置RR?
● 兼容历史包袱（Statement-Based），mysql5.0以前使用statement进行主从复制，如果RC + statement会导致主从数据不一致，RR引入了Gap lock，事务A执行范围操作会锁住，事务B不能够操作，这样保障了从节点按照顺序回放事务A、事务B操作就会数据一致
● 风险大，老应用如果切换RC，可能导致隐藏风险
● 虽然 MySQL 默认是 RR，但很多互联网公司（如阿里、腾讯）在内部实践中，推荐使用 RC + ROW 格式的 Binlog，原因：
  ○ RC 下锁的范围更小，并发性能更好。
  ○ RC 下没有 Gap Lock，死锁概率更低。
  ○ ROW 格式的 Binlog 解决了主从复制的一致性问题。
  ○ 在 Statement 格式下，问题的根源在于从库需要重新执行逻辑，而逻辑的执行结果依赖于当时的快照。在 RC 下，这个快照会变化，导致主从执行时的上下文不同。
  ○ 在 Row 格式下，主库将事务的最终物理结果（即哪些行被改了，改成什么样了） 直接发送给从库。从库不需要理解逻辑，不需要看快照，只需要机械地应用这些行变更。这样，主库上事务提交后的最终状态，就能被无损地复制到从库。
● 还有一种情况，比如事务A在删除id > 90的记录，然后刚好事务A删除92，此时事务B新增了91记录并提交事务，最终主库还是会有91记录，从库回放的时候会执行事务B新增91记录，这再回放A，删除 > 90的记录，91记录被删除了，导致主从数据不一致

两种解决方案
1. RR 隔离级别 + Gap Lock
在 RR 隔离级别下，事务 A 执行 DELETE FROM user WHERE id > 90 时，会锁住 (90, +∞) 这个间隙。事务 B 无法插入 id=91，必须等事务 A 提交。这样就保证了主库上 DELETE 语句的原子性，id=91 要么在删除前存在（被删），要么在删除后存在（保留），不会出现“中途插入”的情况。
2. Row 格式 Binlog
Row 格式不记录 DELETE FROM user WHERE id > 90 这条语句，而是记录实际被删除的每一行：
DELETE FROM user WHERE id=92
DELETE FROM user WHERE id=95
DELETE FROM user WHERE id=100
从库只执行这三条精确删除，不会碰 id=91，结果和主库完全一致。



Mysql Sql执行顺序
FROM → ON → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT
对于Where 条件，优化器有可能判断在On之前执行，筛选更少的数据跟驱动其他表连接，效率更高
Mysql Explain语句
通过explain 优化器分析sql执行过程
列名	含义
id	SELECT 查询的序列标识符
select_type	SELECT 关键字对应的查询类型
table	用到的表名
partitions	匹配的分区，对于未分区的表，值为 NULL
type	表的访问方法
possible_keys	可能用到的索引
key	实际用到的索引
key_len	所选索引的长度
ref	当使用索引等值查询时，与索引作比较的列或常量
rows	预计要读取的行数
filtered	按表条件过滤后，留存的记录数的百分比
Extra	附加信息

type：使用的索引类型
system > const > eq_ref > ref > range > index > all

● system
  ○ 是const 特殊情况，如果表里只有一条数据（或者空表），且存储引擎能够精确统计行数。
● const
  ○ 使用唯一索引、主键索引查询数据时，表中最多只有一条匹配的记录
● eq_ref
  ○ 多用于联表查询，前一张表的行在当前表只有一条记录对应，连表条件为主键或唯一非空索引
  ○ 或用于自联接
● ref
  ○ 使用非唯一索引查询当前表，表中有多条记录
  ○ 联表查询时，前一张表的一条记录在当前表有多行记录
● range
  ○ 使用索引列进行范围查询
● index
  ○ 使用索引树查询，查询整个索引树
  ○ 1、覆盖索引 + 全表扫描，当查询条件满足覆盖索引，并且没有where条件，则扫描整个索引树
    ■ -- 有联合索引 idx_name_age(name, age)SELECT name, age FROMuser;
  ○ 2、使用索引树进行排序
    ■ -- 有索引 idx_create_time(create_time)SELECT*FROMuserORDERBY create_time;
  ○ 3、查询条件导致优化器认为全索引扫描更快
    ■ -- name 有索引，但表中 95% 的数据满足 name LIKE '张%'
    ■ SELECT name FROM user WHERE name LIKE '张%';
    ■ 如果姓张的人占绝大多数，优化器可能认为用索引逐行查找（range 或 ref）还不如直接扫描整个索引树（index）。

rows
● 通过索引扫描后预计需要读取的行数，也是存储引擎返回给Servce的行数

filtered
● 存储引擎返回给servce后，在servce过滤剩下的数据 / rows
● filtered = (条件过滤后的行数 / 存储引擎返回的行数) × 100
● filtered = 100，存储引擎返回的所有行都满足 WHERE 条件（理想情况），表示完全使用了索引，效率高
● filtered < 100，说明索引未完全覆盖查询条件，除了使用索引外，还需要在servce层使用where条件进行过滤数据
● Join场景：优化器用 rows × (filtered / 100) 估算当前表传递给下一张表的行数（扇出）
该字段在多表 JOIN 场景中尤为重要：扇出越大，驱动表需要匹配的被驱动表行数就越多。因此当 filtered 值很低时，说明过滤效率较好；而当 rows 很大且 filtered 又不高时，则是潜在性能瓶颈的信号，应优先考虑通过索引下推（ICP）或更合适的索引来减少扇出。



Extra
包含了Mysql解析查询的额外信息
● Use filesort
  ○ 当查询需要order By、Group By（天然依赖Order By，先排序再分组）排序的时候，如果无法使用索引，则需要另外进行排序，如果结果集大小在 sort_buffer_size以内，则使用内存排序，否则使用磁盘排序
● Use Index
  ○ 使用覆盖索引
● Use Index Condition
  ○ 使用索引下推
● Use Where
  ○ Mysql 对 Servce返回的数据进行了where条件过滤

缓存读写策略（Cache Aside Pattern（旁路缓存模式））
旁路缓存


● 读操作，先从缓存读取数据，没有则读取DB，加载到缓存
● 写操作，先把数据更新到DB，然后删除缓存

1. 可以先删除缓存，再写入DB吗
  a. 不可以，会造成高并发，大量数据打到数据库，造成数据库连接池满
  b. 可能会造成数据不一致，如果删除缓存的时候，同时有一个B读请求，读到了旧数据，然后将旧数据加载到了缓存，A请求写完了DB，数据会不一致
2. 先更新DB，再删除缓存就会数据一致吗
  a. 不一定，但发生概率低，时间窗口小
  ○ 时序分析 (请求 A 读, 请求 B 写):
    ⅰ. 请求 A : 缓存未命中，从 DB 读取到旧值。
    ⅱ. 请求 B: 迅速完成了 DB 的更新，并将 Cache 删除。
    ⅲ. 请求 A : 将自己之前拿到的旧值写入了 Cache。
  ○ 结果： DB 中是新值，Cache 中又是旧值。
3. 可以更新缓存，而不是删除吗
  a. 更新成本更大，需要计算更新的新对象，而删除是轻量级操作，快捷轻便
  b. 懒加载思想，当用到的时候才加载

读写穿透（Read/Write Through Pattern）
读写都直接操作cache
● 写操作：直接写入cache，由Cache服务自己写入DB或者由服务使用者自行实现cache -> db的更新
● 读操作：读取cache，如果cache不存在则读db，加载到cache

异步缓存写入（Write Behind Pattern）
异步缓存更新，跟读写穿透不一样，读写穿透是同步
● 不适用强一致性
● 写操作：写入到cache，将写数据丢到消息队列，通过异步线程/任务批量写入db
● 读操作，直接读取cache，如果cache不存在则读db，加载到cache
● MySQL 的 InnoDB Buffer Pool 机制： 数据修改先在内存 Buffer Pool 中完成，然后由后台线程异步刷写到磁盘。
● 操作系统的页缓存（Page Cache）： 文件写入也是先写到内存，再由操作系统异步刷盘。
● 高频计数场景： 对于文章浏览量、帖子点赞数这类允许短暂数据不一致、但写入极其频繁的场景，可以先在 Redis 中快速累加，再通过定时任务异步同步回数据库。

Redis持久化机制
RDB
某个时间的redis数据快照，redis默认持久化机制
● 开启生成快照的命令
  ○ save：主线程生成rdb快照，阻塞主线程
  ○ bgsave：fork一个子进程生成rdb快照，也会阻塞主线程一段时间，主要复制页表
  ○ 配置
    ■ # save 3600 1# save 300 100# save 60 10000
    ■ save 3600 1 ← 条件1：1小时内，只要改过 1 个 key
    ■ save 300 100 ← 条件2：5分钟内，改过 100 个 key
    ■ save 60 10000 ← 条件3：1分钟内，改过 10000 个 key
● 主进程、子进程共享同一个物理数据，通过COW，如果主进程修改某个key，则主进程生成一个新页存储key的新值
  ○ 
● RDB持久化流程
  ○ redis数据满足持久化条件，出发bgsave，阻塞主进程，生成子进程，复制页表，新、子指向同一个物理存储空间
  ○ 子进程将物理数据序列化生成快照rdb文件
  ○ 操作系统通过rename()原子操作，将快照rdb重命名为dump.rdb文件
  ○ 子进程任务结束，清理COW没有被引用的旧页，释放内存

AOF
所有redis的写命令都会追加到AOF缓冲区
● AOF持久化流程
  ○ 命令追加：Redis写命令，追加到AOF缓冲区
  ○ 文件写入：调用write方法，将AOF缓冲区数据同步到系统内核缓冲区
  ○ 文件同步：根据appendfsync策略，redis在不同的时期调用fsync方法，将内核缓冲区的AOF数据持久化到磁盘
  ○ 文件重写：当AOF文件不断变大，会对AOF进行重写
  ○ 重启加载：Redis重启，可以加载AOF文件进行数据恢复

● AOF刷盘策略
  ○ appendfsync always：redis每次write完，都由主线程调fsync刷盘，会阻塞主线程，效率极低
  ○ appendfsync everysec：由子线程每秒调用fsync刷盘，效率高，如果redis宕机，会有1s数据的丢失
  ○ appendfsync no：由操作系统决定什么时机调用fsync，一般是30s

RDB和AOF对比
● RDB
  ○ 二进制文件，占用内存小，恢复速度快，适合主从复制
● AOF
  ○ 数据更安全，可实现实时和秒级数据持久化
  ○ 版本兼容性好，不同redis版本会有多种RDB类型

混合持久化
RDB和AOF结合，全量备份 + AOF增量
恢复时：先加载 RDB 快照部分（速度极快）再重放后面追加的增量 AOF 命令（量很小，通常只有几MB）总恢复时间 ≈ RDB加载 + 极少量的AOF重放

选型建议



造成Redis阻塞的原因
● O(n)命令
  ○ Keys * ：返回所有符合规则的key
  ○ HGETALL：获取一个hash所有的键值对
● 生成RDB快照
  ○ 通过Save命令，主线程生成RDB快照
● AOF阻塞
  ○ AOF刷盘阻塞，上一次fsync超过2s，导致主线程不接收新的请求
  ○ AOF追加日志阻塞，redis操作完命令后，主线程会将命令追加到AOF缓冲区
● 大Key阻塞
  ○ 查找、删除大Key
  ○ Redis是单线程，操作大key比较耗时
  ○ 使用del删除大key，会阻塞工作线程，无法处理后续命令
  ○ 网络阻塞，如果一个大key 1mb，每秒访问量1000，那么1s产生1000m的流量，对于千兆网卡的服务器是灾难性的
● 清空数据库
  ○ flushdb、flushall
Redis 大Key
查找大key
● 通过 --bigkeys 命令，只能查找每个数据类型 top 1的大key
● 


@Lazy注解
懒加载，只有需要的时候才加载（实例化、初始化），打破循环依赖
被Lazy注解的bean，在属性注入的时候会生成延迟代理对象，只有真正调用这个bean的方法的时候才进行加载

Bean的生命周期
1. bean实例化
  a. 反射创建对象
2. 属性赋值
  a. 字段、依赖赋值
3. Bean初始化
  a. 是否实现Aware方法，设置bean名称等
  b. 是否实现BeanPostProcessor方法，执行前置处理
  c. 是否实现InitializingBean接口，执行AfterPropertiesSet方法
  d. 是否实现BeanPostProcessor方法，执行后置处理
    ⅰ. postProcessAfterInitialization 方法，生成代理对象
4. bean销毁
  a. 如果实现了DisposableBean接口，执行destory方法
  b. 如果注解@Predestory，先执行销毁前方法

Spring AOP
实现都是通过 Spring AOP + AspectJ注解，底层是动态代理


三级缓存
三个Map
1. 一级缓存
  a. 存放已经实例化、属性注入、初始化完成的bean
2. 二级缓存
  a. 存放早期暴露引用，属性未注入的半成品bean，可能是普通bean或者代理bean
3. 三级缓存
  a. 存放ObjectFactory，创建早期暴露对象引用放入二级缓存

属性注入过程
● 先从一级缓存查找是否有对应bean，如果没有则到二级缓存找bean，如果还没有则到三级缓存寻找bean，

循环依赖创建 A：
1. 实例化 A（原始对象 A@123）
2. 放入三级缓存：singletonFactories.put("a", factory)
// 注意：此时二级缓存还是空的！
3. 开始属性填充：发现需要 B
创建 B：
1. 实例化 B（原始对象 B@456）
2. 放入三级缓存：singletonFactories.put("b", factory)
3. 开始属性填充：发现需要 A
4. 获取 A：
  ○ 一级缓存：没有
  ○ 二级缓存：没有  ← 关键：A 还没有进入二级缓存
  ○ 三级缓存：有 factory
  ○ 调用 factory.getObject() → 创建 ProxyA@789
  ○ 放入二级缓存：earlySingletonObjects.put("a", ProxyA@789)
  ○ 移除三级缓存：singletonFactories.remove("a")
5. 注入 ProxyA@789 到 B
6. B 初始化完成
7. B 放入一级缓存
继续创建 A：
  4. 注入 B 到 A（原始对象 A@123）
  5. A 初始化完成
  6. 检查是否需要代理
  7. 已有早期代理 ProxyA@789，不再重复创建
  8. A 放入一级缓存：singletonObjects.put("a", ProxyA@789)

事务隔离级别
以数据库设置的为准，其次是spring @Transactional

RocketMQ架构
https://www.cnblogs.com/qdhxhz/p/11094624.html
● MessageQueue 只会被一个消费者组中的一个消费者消费
● 消息实际存储在broker的commitLog中，queue存储的offset是逻辑索引，实际commitLog存储消息（物理数据），这样消息存储到commnitLog顺序写入，速度快，且高可用
● 生产者、消费者 每隔30s从namseServer获取对应topic的broker映射
● broker 每隔30s同步其下所有的topic信息给所有的namserver
● namaserver 每隔10s检测存活的broker，如果2min broker没有同步topic信息，则将broker信息摘除
● 通信过程都是保持长连接
● 消费者消费模式默认是并发消费
● 消息默认是push模式（消费者本地开启监听消息的线程，当queue有消息时，则主动拉取一份消息到本地，通过offset控制消息消费位置）
● offset 每隔 5s会更新到broker下的某个topic的queue中
● 一个MessageQueue对应一个消费者实例，这个消费者实例默认20个线程共同消费同一个broker存储topic下的同一个MessageQueue
● MessageQueue是逻辑消息，ConsumerQueue是存储索引的地址，一个MessageQueue对应一个目录下的ConsumerQueue，通过ConsumerQueue中的索引找到CommitLog中真正的消息信息
● rebalance触发场景
  ○ 消费者实例上线、下线
  ○ queue数量变化
  ○ broker上下线






NameServer 管理的是 Topic → Broker 的路由配置，而 Topic → Queue 的选择由生产者和消费者在本地完成。

消息发送过程


RocketMq基于什么进行选型？
我们主要调研了几个主流的mq，kafka、rabbitmq、rocketmq、activemq，选型我们主要基于以下几个点去考虑：
1. 由于我们系统的qps压力比较大，所以性能是首要考虑的要素。
2. 开发语言，由于我们的开发语言是java，主要是为了方便二次开发。
3. 对于高并发的业务场景是必须的，所以需要支持分布式架构的设计。
4. 功能全面，由于不同的业务场景，可能会用到顺序消息、事务消息等。
基于以上几个考虑，我们最终选择了RocketMQ。


消息的可靠性怎么保证？

生产者：
1. 简单点的，通过失败重试机制，用的最多，失败二次重试
2. 异步消息 + 消息回调 + 本地消息表 + 定时任务
  a. 发送消息
  b. 判断消息是否成功
  c. 成功则结束，失败持久化到本地消息表
  d. 定时任务定时扫描失败消息，重新投递

MQ：
1. 同步刷盘、异步刷盘，持久化到磁盘
2. 主从架构，将消息同步到从节点，避免单点故障造成消息丢失

消费者：
1. 消息确认机制：CONSUMER_SUCEES、CONSUMER_LATER
2. 消息重试机制：消费失败 -> 重新投递（默认16次）-> 成功则成功，失败进入私信队列（需要手动处理）
3. 由开发者自行实现幂等性
  a. msgId 幂等
  b. 业务纬度加锁 + 反查最新状态
  c. 数据库唯一键

消息堆积了怎么办？
https://www.cnblogs.com/wzh2010/p/15888534.html
● 紧急，快速止血
  ○ 扩容，提高消费组数量，快速消费消息
  ○ 跳过非核心的错误消息，保证其他消息先成功
  ○ 消息转发，新建topic专门用来处理异常消息，保证下游某个服务宕机不拖垮主链路
● 根治
  ○ 消费者线程不够
  ○ 数据库慢sql，处理速度过慢
  ○ 下游服务rt高，导致消息处理慢
  ○ 内存泄漏等导致的频繁full gc
  ○ 死锁，线程之间互相等待，查看数据库死锁、分布式锁死锁

事务消息、半事务消息
● 发送事务消息，没有二次确认的事务消息为半事务消息
● 生产者发送事务消息到broker
● MQ收到后返回ack
● 生产者执行本地事务，执行成功则二次commit，失败则rollback，事务消息根据生产者发送commit、rollback决定发送消息还是回滚
● 如果MQ长时间未收到生产者的二次确认，则MQ会对生产者发起消息回查，调用 checkLocalTransaction 方法，查询本地事务状态
● 查询生产者的本地事务状态，进行二次确认


一次性导出10w条数据，要怎么做？
● 创建异步任务
● 游标卡尺分页查询，避免一次性加载过多数据到内存，同时避免数据库深分页问题
● 通过Easyexcel流式持久化到硬盘
● 上传本地文件到OSS
● 下载文件到邮箱，或者直接给前端提供oss下载链接


Top K / 频次过滤
如果现在让你写一个算法，多个用户有不同的用户 ID，他们的请求次数不一样。然后需要你把其中请求次数大于 K 的用户 ID 都筛选出来，那你要怎么设计

1、数据量不多
● 全部加载到内存，通过HashMap统计每个userId出现的次数
● 在遍历所有的entry，找到出现次数 > k 的用户id

2、数据量大，用户量不会过多（100G数据）
● MapReduce，通过将100G数据分为1000个小文件，每个100m
● 多线程循环处理每个小文件，将最终的结果存入磁盘，同时按照userId排序（保证归并的时候能把同一个元素所有值都找到）
● 归并操作，单线程循环读取每个结果文件的第一行数据，建立小顶堆，每个元素存储userId：count，将相同id弹出堆顶，然后相加count，如果最后count值 > k，则持久化到磁盘

3、数据量大，且用户量非常多
● 分桶排序
● 循环读取数据，比如分1000个桶，通过哈希函数将相同id数据放到一个桶中
● 循环处理不同桶数据，将内部通过userId排序
● 循环遍历一个桶的数据，因为通过哈希函数，同一个用户id一定在同一个桶中，并且进行了排序，这样就可以遍历一个桶，然后将userId出现的次数统计出来，直到遇到下一个userId结束，判断当前userId 出现次数是否 > k，如果大于则持久化到磁盘


线上遇到的问题

慢sql怎么处理的？






Arthas trace原理
Arthas 监控方法耗时的实现原理：
1. Attach API：动态附加到目标 JVM。
2. Instrumentation API：注册 ClassFileTransformer。
3. ASM 字节码：修改目标方法的字节码。
4. 插入代码：在方法开始和结束插入计时逻辑。
5. 数据收集：通过 ThreadLocal 记录方法调用栈。
6. 异步上报：批量异步上报监控数据。
核心技术栈：
● Java Attach API：动态附加到目标进程。
● Java Instrumentation：动态修改字节码。
● ASM：字节码操作框架。
● ThreadLocal：线程安全的数据存储。











高管反问
1. 目前团队遇到最大的挑战或最想提升的东西是什么？
2. 作为leader，对于员工的考核标准是，以及哪些是加分项？
3. 公司ai基建怎么样？有没有要求ai coding占比？以及有没有对应的skill、mcp发布平台？
4. 团队的技术氛围怎么样？有没有定期组织技术分享会？
5. 如果我加入，我想负责什么领域？你对我会有什么期望？




你从公司学到了什么



